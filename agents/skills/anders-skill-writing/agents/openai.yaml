interface:
  display_name: "Anders Skill Writing"
  short_description: "Apply Anders's evolving skill preferences"
  default_prompt: "Use $anders-skill-writing to draft this skill from Anders's current preferences without freezing open choices."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
