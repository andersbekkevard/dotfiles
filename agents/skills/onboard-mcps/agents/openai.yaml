interface:
  display_name: "Onboard MCPs"
  short_description: "Reconcile a machine with the private MCP registry"
  default_prompt: "Use $onboard-mcps to onboard my registered MCPs on this machine."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
