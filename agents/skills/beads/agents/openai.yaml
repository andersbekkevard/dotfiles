interface:
  display_name: "Beads Protocol"
  short_description: "How to use br: states, labels, lifecycle, permissions"
  default_prompt: "Use $beads for the beads protocol — states, labels, required fields, and the verification lifecycle."
