interface:
  display_name: "Eval"
  short_description: "Blindly evaluate an agent-behavior variant"
  default_prompt: "Use $eval to test this variant with blinded candidates and one calibrated judge."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
