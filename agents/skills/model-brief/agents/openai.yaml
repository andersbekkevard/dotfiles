interface:
  display_name: "Model Brief"
  short_description: "Assemble a prompt for stronger-model guidance"
  default_prompt: "Use $model-brief to assemble a self-contained prompt file for stronger-model guidance."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
