interface:
  display_name: "Model Brief"
  short_description: "Preserve user intent in a stronger-model prompt"
  default_prompt: "Use $model-brief to assemble a self-contained stronger-model prompt with reconstructed user intent and verbatim user anchors."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
