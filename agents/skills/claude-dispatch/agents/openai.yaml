interface:
  display_name: "Claude Dispatch"
  short_description: "Run a ready prompt through Claude Code"
  default_prompt: "Use $claude-dispatch to run this ready prompt with the access boundary I specify."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
