interface:
  display_name: "Personal Edge"
  short_description: "Share apps privately or publish durable URLs"
  default_prompt: "Use $personal-edge to share this app over SSH by default, publish it publicly only when explicitly requested, or operate mail on bekkevard.me."
