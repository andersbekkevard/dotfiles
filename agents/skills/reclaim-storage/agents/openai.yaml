interface:
  display_name: "Reclaim Storage"
  short_description: "Audit machine storage and propose safe reclaim"
  default_prompt: "Use $reclaim-storage to audit this machine and show what could be removed safely."
