interface:
  display_name: "Record Skill Use"
  short_description: "Save one real skill use as evidence"
  default_prompt: "Use $record-skill-use to preserve this skill use and any feedback as durable evidence."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
