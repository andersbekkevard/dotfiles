interface:
  display_name: "Codex Dispatch"
  short_description: "Run a ready prompt through Codex"
  default_prompt: "Use $codex-dispatch to run this ready prompt with the access boundary I specify."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
