interface:
  display_name: "Define Goal"
  short_description: "Shape clear measurable goals"
  default_prompt: "Use $define-goal to turn this intention into a concrete, measurable goal before starting work."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
