interface:
  display_name: "Provision Hetzner Dev Server"
  short_description: "Provision a rescue host as a dev server"
  default_prompt: "Use $provision-hetzner-dev-server to provision root@203.0.113.10 as atlas."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
