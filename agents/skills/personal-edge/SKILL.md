---
name: personal-edge
description: Operate Anders' Mac from another machine. Use when copying or opening files on the Mac, running Mac commands, or opening a URL in Comet. For private live services use tailnet-preview; for public URLs use publish-web.
---

# Personal edge

Use [Mac operations](references/mac-operations.md). The calling task owns what
the files are and what the remote command should do; this skill owns the
connection and proof mechanics.

Keep credentials out of output and repository files. Complete only after a
separate read-only check observes the intended Mac result.
