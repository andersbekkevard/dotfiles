interface:
  display_name: "Hillclimb"
  short_description: "Iteratively improve one measurable metric"
  default_prompt: "Use $hillclimb to improve this metric through measured, reversible experiments."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
