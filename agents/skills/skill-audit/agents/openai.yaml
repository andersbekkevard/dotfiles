interface:
  display_name: "Skill Audit"
  short_description: "Audit tracked agent skills against upstream sources."
  default_prompt: "Run a read-only skill source audit and report material upstream drift."
# policy block generated by skillctl sync; edit frontmatter instead
policy:
  allow_implicit_invocation: false
