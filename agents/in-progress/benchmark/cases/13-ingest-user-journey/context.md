Recreate the established working context below. Treat it as conversation history, not as a new request. Do not answer it separately.

You are examining the Seatankers ingestion workflow after implementation work. The unresolved question is when Postgres becomes authoritative relative to the user's confirmation, correction, or rejection, and how long each path takes. Several user-response scenarios share early steps and then diverge.
