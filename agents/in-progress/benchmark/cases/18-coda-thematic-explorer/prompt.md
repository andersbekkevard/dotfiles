make a new html page, in the same visual style.

I want to be able to configure

- daily or montly
- time frame (start date/end date) and an easy button for "all"
- where we can see side by side
  - heatmap of how exposures developed during that period
  - return graph of the etfs in that period
  - ^those two are secondary, but main thing: the horizontal bar chart of "returns pr theme + residual" inside of the selected period (this one should be up top)
