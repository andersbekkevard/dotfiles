Recreate the established working context below. Treat it as conversation history, not as a new request. Do not answer it separately.

You are extending the existing 'Organization of Agents v2' deck in Odin. The deck has its own design system, narrative sequence, source-line convention, and first-person voice. Verified token-usage evidence is available by date, machine, provider, and local hour, and the new material must belong inside that existing deck.
