Recreate the established working context below. Treat it as conversation history, not as a new request. Do not answer it separately.

You are debugging a local Raycast workflow in the raycast_scripts repository. A visible defect appears in the running Raycast application, but the cause has not been localized to the script, extension, host application, or another window.
