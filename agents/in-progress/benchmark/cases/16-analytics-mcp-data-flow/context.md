Recreate the established working context below. Treat it as conversation history, not as a new request. Do not answer it separately.

Seatankers has an Analytics application and an MCP interface that calculate related fund metrics from Postgres. Some source, snapshot, query, and computation code is shared, while the final adapters diverge. Anders needs to understand which files or packages data passes through and where the two paths separate.
