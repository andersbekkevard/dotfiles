Recreate the established working context below. Treat it as conversation history, not as a new request. Do not answer it separately.

You are reviewing Anthropic's public html-effectiveness repository, which contains a gallery of standalone HTML examples. Anders wants a convenient local way to inspect every example in the cloned repository from his Mac.
