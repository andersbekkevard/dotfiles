Recreate the established working context below. Treat it as conversation history, not as a new request. Do not answer it separately.

You are running an overnight Seatankers benchmark of the bronze-to-gold ingestion pipeline. The campaign replays raw fund files through an agent-equivalent tool and skill environment, scores multiple models, records failures and fixes, and must prove the end-to-end email-to-ingestion journey against a disposable Postgres system without overfitting to the sampled funds.
