interface:
  display_name: "Project"
  short_description: "Shape and maintain Anders project repositories"
  default_prompt: "Use $project to create, maintain, or audit this project's durable context contract."
